# Gitignored local files copied from the main checkout into each new worktree
# on create — the ones a fresh checkout won't have but a build needs.
# One path per line, relative to the repository root.
# Blank lines and lines starting with # are ignored.
# Edits take effect on the next create — nothing to restart.

.claude/settings.local.json
local.properties

